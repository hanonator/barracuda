package org.barracuda.model.actor.sync.attribute;

/**
 * Should be hidden from the user a bit
 * 
 * @author brock
 *
 */
public class HitPrimary extends AbstractHit {

	public HitPrimary(int amount) {
		super(amount);
	}

	public HitPrimary(int amount, Sprite sprite) {
		super(amount, sprite);
	}

}
